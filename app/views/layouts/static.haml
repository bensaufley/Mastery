!!!
%html
  %head
    %meta{ name: 'viewport', content: 'initial-scale=1, maximum-scale=1' }
    %title Mastery
    = stylesheet_link_tag 'application', media: 'all'
    = csrf_meta_tags
    / [if lt IE 9]
      <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
      -# <script src="https://oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js"></script>
  %body.static
    =render 'layouts/navigation'
    - if content_for?(:jumbotron) || content_for?(:title)
      %header.jumbotron
        .container
          = content_for(:jumbotron) if content_for? :jumbotron
          - if !user_signed_in?
            .row
              .col-sm-4.col-sm-offset-4
                = link_to 'Sign Up', new_user_registration_path, class: 'btn btn-lg btn-primary btn-block'
    %main{ role: 'main' }
      .container
        %header
          - if !content_for?(:right) && !flash.empty?
            =render 'layouts/notifications'
        %section= yield
    %footer
      .container
        %p Copyright ©2013 Ben Saufley
    = javascript_include_tag "application"